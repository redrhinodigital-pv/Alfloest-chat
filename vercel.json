{
  "installCommand": "echo \"No install step\"",
  "buildCommand": "if test -d \"./flutter\"; then ./flutter/bin/flutter upgrade; else git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
